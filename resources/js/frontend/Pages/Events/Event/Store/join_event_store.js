import { defineStore } from "pinia";
import axios from "axios";

export const store = defineStore("join_event_page", {
  state: () => ({
    loading: false,
    error: null,
    success: false,
    
  }),
  actions: {
   
  },
});
